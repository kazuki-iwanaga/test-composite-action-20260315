---
# Source: cloudrun-service/templates/service.yaml
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: basic-service
spec:
  template:
    spec:
      containers:
        - image: nginx:latest
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: "1000m"
              memory: "512Mi"
